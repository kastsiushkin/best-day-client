import { moduleFor, test } from 'ember-qunit';

moduleFor('route:days/index', 'Unit | Route | days');

test('days/index route exists', function(assert) {
  var route = this.subject();
  assert.ok(!route, 'days/index route exists');
});
